pipeline {
    agent any
    stages {
        stage('Clean') {
            steps {
            sh 'mvn clean'
            }
        }
	
	
	stage('SystemTest') {
            steps {
            git url: 'https://github.com/shawtrig/EMSystemTests-ex.git'
	    sh 'mvn -Dtest=ExpenseManagerSystemTest test'
            }
        }
	
    }
}
